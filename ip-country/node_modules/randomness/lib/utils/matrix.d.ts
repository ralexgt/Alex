import { Bit, Matrix } from '../types';
export declare const bitsToMatrix: (M: number, Q: number, bits: Bit[]) => Matrix;
export declare const computeRank: (M: number, Q: number, matrix: Matrix) => number;
